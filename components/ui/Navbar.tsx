import { Post } from "@/interfaces/post";

export default function Navbar() {
    return (
        <nav className="w-full dark:bg-black  p-4">
            <h1 className="text-2xl  text-white">Navbar</h1>
        </nav>
    );
}
